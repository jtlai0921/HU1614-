Module Module1

    Sub Main()
        Dim intX, intY, intZ As Integer
        intX = 10
        intY = 20
        intZ = intX + intY
        Console.WriteLine("intX + intY = " & intZ)
        Console.Read() '暫停
    End Sub

End Module
